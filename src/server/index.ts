export { encstreamMiddleware as expressMiddleware } from './middleware/express';
export { encstreamMiddleware as nextMiddleware } from './middleware/next';

// Export common types
export type { EncryptionConfig, EncryptedPayload } from '../core/types';
